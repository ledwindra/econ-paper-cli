# Auto-detect text files and normalize line endings to LF
* text=auto eol=lf

# Force LF line endings for JSON and manifest files so checksums match cross-platform
*.json text eol=lf
*.manifest.json text eol=lf
